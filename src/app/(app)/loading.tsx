import { LoadingState } from "@/components/ui/loading-state";

export default function AppLoading() {
  return (
    <LoadingState
      description="Preparing the protected demo workspace view."
      title="Loading OpsPilot"
    />
  );
}
